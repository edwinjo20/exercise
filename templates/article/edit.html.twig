{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5">
        <h1>Edit Article</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary mt-3">Save Changes</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
