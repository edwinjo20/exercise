{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create New Category</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary mt-3">Save</button>
    {{ form_end(form) }}
{% endblock %}
